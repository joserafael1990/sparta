<%= search_form_for @search do |f| %>	
	<div class="form-group">		
		<%= f.text_field :name_cont, autofocus: true, placeholder: 'Ingrese nombre' %>
		<%= f.submit 'Buscar', class: "btn btn-link"  %>
		<%= link_to 'Nueva persona', new_person_path, {:class=>"btn btn-info"} %>
	</div>
<% end %>