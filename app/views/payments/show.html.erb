<%=render 'layouts/admin'%>

<div class="col-xs-12 col-sm-6 col-md-8">
	<br>
	<h2>Ver pago</h2> 
	<p>
		<strong>Nombre:</strong>
		<%= @payment.person.name + ' '+ @payment.person.last_name %>
	</p>
	<p>
		<strong>Fecha:</strong>
		<%= @payment.created_at %>
	</p>
	<p>
		<strong>Monto:</strong>
		<%= @payment.amount %>
	</p>
	<p>
		<strong>Motivo:</strong>
		<%= @payment.description %>
	</p>
	<p>
		<%= link_to 'Atrás', payments_path , {:class=>"btn btn-link"}%> | <%= link_to 'Editar', edit_payment_path(@payment), {:class=>"btn btn-link"} %>
	</p>
</div>

